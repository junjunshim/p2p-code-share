Non-Server Based 동시 편집 VS Code 확장 프로그램 개발 계획서
1. 프로젝트 개요
프로젝트명: P2P Code Share (Test MVP)

목적: 중앙 서버 없이 동일 네트워크 환경(LAN) 내에서 두 대의 PC가 WebRTC와 Yjs를 활용하여 실시간으로 코드를 동시 편집하고 커서를 공유할 수 있는지 검증

개발 언어 및 환경: TypeScript, Node.js, VS Code Extension API

2. 핵심 구현 목표 (MVP)
WebRTC 다이렉트 연결: 외부 서버 없이 두 클라이언트가 대화할 수 있는 데이터 채널(Data Channel) 개설

실시간 텍스트 동기화: Yjs(CRDT)를 이용해 타이핑 시 발생하는 충돌을 양방향으로 자동 해결

원격 커서 및 이름표 표시: 상대방의 마우스 포인터 위치를 에디터 위에 실시간 렌더링

가상 권한 검증 기능: 호스트(Host)가 게스트(Guest)의 편집 권한(24비트 마스크 시뮬레이션)을 로컬에서 판별

3. 기술 스택 및 라이브러리
Extension Core: vscode API

CRDT 엔진: yjs (텍스트 변경 사항 추적 및 병합)

WebRTC 래퍼: simple-peer 또는 wrtc (VS Code 데스크톱 환경은 Node.js 기반이므로 필요)

바인딩 라이브러리: y-protocols (Awareness 모듈을 통한 커서 동기화용)

4. 시스템 아키텍처 및 데이터 흐름
초기 연결 (시그널링) 흐름
호스트가 Offer SDP 생성 -> 메신저 등으로 게스트에게 텍스트 직접 전달 -> 게스트가 Answer SDP 입력 및 호스트에게 반사 -> WebRTC P2P 고속도로 연결 완료

편집 데이터 흐름
게스트가 타이핑 수행 -> 게스트의 Yjs가 바이너리 변환 -> WebRTC DataChannel로 전송 -> 호스트의 로직이 게스트의 24비트 권한 체크 -> 승인 시 호스트 화면에 적용 및 타 클라이언트에 전송

5. 단계별 개발 일정 (5단계)
Phase 1: 개발 환경 세팅 및 UI 구성

태스크: yo code를 이용해 TypeScript 기반 VS Code Extension 기본 구조 생성. Side Bar에 "공유방 만들기 (Host)", "참여하기 (Guest)" 버튼 추가

결과물: 기본 버튼과 입력창이 포함된 VS Code 확장 프로그램 패키지

Phase 2: 순수 WebRTC 데이터 채널 테스트

태스크: simple-peer 라이브러리를 활용해 암호화된 P2P 통신망 구현. 호스트가 생성한 Offer 문자열을 게스트가 복사해서 넣고, 게스트가 만든 Answer를 호스트가 다시 입력하는 수동 연결 UI 구현

검증: 두 컴퓨터의 확장 프로그램 로그 창에 "P2P Connection Established!" 문구가 뜨고 간단한 텍스트 채팅이 전송되는지 확인

Phase 3: Yjs 텍스트 동기화 엔진 결합

태스크: 현재 활성화된 VS Code 에디터 문서의 변경 이벤트를 가로챔. 이 변경 사항을 Y.Doc에 반영하고, 여기서 나오는 바이트 배열(Uint8Array)을 WebRTC Data Channel로 송수신

검증: 호스트가 타이핑하면 게스트의 에디터에 글자가 똑같이 쳐지고, 동시에 타이핑해도 글자가 깨지지 않고 병합되는지 확인

Phase 4: 원격 커서 및 가상 권한 로직 추가

태스크: y-protocols/awareness를 연동하여 커서의 Line, Character 위치 정보를 P2P로 공유. vscode API를 이용해 상대방 이름표와 커서 렌더링. 호스트 컴퓨터에 게스트의 권한을 Viewer(0x000001) 혹은 Editor(0x000002)로 가상 설정해두고, 비트 연산 결과에 따라 게스트의 편집 데이터를 무시하는 필터링 코드 삽입

검증: 마우스 움직임이 실시간으로 보이고, Viewer 권한인 상태에서 강제로 수정을 시도할 때 차단되는지 확인

Phase 5: 최종 통합 테스트 및 검증

태스크: 동일 공유기(LAN) 환경에 있는 팀원 컴퓨터 2대를 준비하여 전체 시나리오 작동 여부 테스트

6. 최종 확인용 테스트 시나리오 (Checklist)
SDP 교환 연결: 복사/붙여넣기 과정을 거친 후 외부 서버 없이 두 컴퓨터가 P2P로 직접 연결된다.

실시간 동시 타이핑: 양쪽에서 동시에 각자 다른 줄을 편집했을 때, 충돌 없이 코드가 실시간으로 합쳐진다.

커서 트래킹: 상대방이 방향키를 움직이거나 마우스를 클릭할 때 이름표가 달린 가짜 커서가 부드럽게 이동한다.

24비트 가상 권한 제어: 호스트가 게스트 권한을 Viewer로 바꾼 비트 상태일 때, 게스트의 편집 시도가 호스트 화면에 반영되지 않고 거부된다.